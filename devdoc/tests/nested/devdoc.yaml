package:
  title: Nested Fixture
  description: Exercises multi-level nesting, tables, footnotes and admonitions.
  language: en
  root: src

build:
  build-dir: book
  create-missing: true

output:
  html:
    fold:
      enable: true
      level: 1

